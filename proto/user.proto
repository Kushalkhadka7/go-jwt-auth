syntax = "proto3";

package pb;

option go_package = ".;___pb";

message User {
    enum Role{
        ADMIN = 0;
        USER = 1;
    }

    string name = 1;
    string password = 2;
    Role role = 3;
}
